# k8s-log-aggregator
k8s-log-aggregator is a Python CLI tool that simplifies debugging in Kubernetes by collecting and aggregating logs from multiple pods using label selectors. It supports parallel log fetching, regex filtering, and JSON/file export, making it easier to analyze distributed system issues.
